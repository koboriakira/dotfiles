{
  "globalShortcut": "Shift+Alt+Space",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--headless",
        "--vision"
      ]
    }
  }
}
